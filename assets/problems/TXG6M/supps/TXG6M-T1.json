{
    "Title": "Checking Account Fees at `text(Two)` Banks",
    "Headers": ["Bank", "`text(Monthly)` Fee"],
    "Rows": [
        ["`text(F)`", "`$8` when the average `text(daily)` balance is less than `$500`; no fee when it is `$500` or more"],
        ["`text(K)`", "`$12` when the average `text(daily)` balance is less than `$750`; no fee when it is `$750` or more"]
    ]
}